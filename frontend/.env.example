# Public API base URL (omit in dev if Vite proxy targets the backend)
# VITE_API_URL=http://localhost:8000

# Optional: sent as login_secret on POST /api/auth/login when backend AUTH_LOGIN_SECRET is set
# VITE_AUTH_LOGIN_SECRET=
